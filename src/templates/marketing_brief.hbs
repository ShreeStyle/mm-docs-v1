<!DOCTYPE html>
<html>

<head>
    <style>
        {
                {
                    {
                    brandCSS
                }
            }
        }

        body {
            font-family: var(--font-primary), sans-serif;
            padding: 50px;
            max-width: 900px;
            margin: 0 auto;
        }

        .header {
            background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
            color: white;
            padding: 40px;
            border-radius: 10px;
            margin-bottom: 40px;
        }

        .logo {
            max-height: 60px;
            margin-bottom: 20px;
        }

        h1 {
            margin: 0;
            font-size: 2.5em;
        }

        .subtitle {
            font-size: 1.2em;
            opacity: 0.9;
            margin-top: 10px;
        }

        .section {
            background: #f9f9f9;
            padding: 30px;
            margin-bottom: 20px;
            border-left: 5px solid var(--accent-color);
        }

        .section-title {
            color: var(--primary-color);
            font-size: 1.8em;
            margin-bottom: 15px;
        }

        .objective-box {
            background: var(--secondary-color);
            color: white;
            padding: 20px;
            border-radius: 5px;
            font-size: 1.1em;
        }

        .strategy-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
            margin-top: 20px;
        }

        .strategy-item {
            background: white;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .strategy-item h3 {
            color: var(--primary-color);
            margin-top: 0;
        }
    </style>
</head>

<body>

    <div class="header">
        {{#if brandKit.logo}}
        <img src="{{brandKit.logo}}" class="logo" />
        {{/if}}
        <h1>{{content.title}}</h1>
        <div class="subtitle">{{content.subtitle}}</div>
    </div>

    <div class="section">
        <h2 class="section-title">Campaign Objective</h2>
        <div class="objective-box">
            {{content.objective}}
        </div>
    </div>

    <div class="section">
        <h2 class="section-title">Target Audience</h2>
        <p><strong>Demographics:</strong> {{content.audience.demographics}}</p>
        <p><strong>Psychographics:</strong> {{content.audience.psychographics}}</p>
        <p><strong>Pain Points:</strong> {{content.audience.painPoints}}</p>
    </div>

    <div class="section">
        <h2 class="section-title">Marketing Strategy</h2>
        <div class="strategy-grid">
            {{#each content.strategies}}
            <div class="strategy-item">
                <h3>{{this.channel}}</h3>
                <p>{{this.description}}</p>
            </div>
            {{/each}}
        </div>
    </div>

    <div class="section">
        <h2 class="section-title">Timeline & Budget</h2>
        <p><strong>Duration:</strong> {{content.timeline}}</p>
        <p><strong>Budget:</strong> {{content.budget}}</p>
    </div>

</body>

</html>