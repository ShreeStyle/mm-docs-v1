<!DOCTYPE html>
<html>
<head>
  <style>
    {{{brandCSS}}}
    body {
      font-family: var(--font-primary), sans-serif;
      color: #333;
      padding: 40px;
    }
    h1 { color: var(--primary-color); }
    h2 { color: var(--secondary-color); border-bottom: 2px solid var(--accent-color); }
    .logo { max-height: 80px; margin-bottom: 20px; }
    .footer { margin-top: 50px; font-size: 0.8em; color: #777; }
  </style>
</head>
<body>
  {{#if brandKit.logo}}
    <img src="{{brandKit.logo}}" class="logo" alt="Logo" />
  {{/if}}
  
  <h1>{{title}}</h1>
  <p><strong>Date:</strong> {{generatedDate}}</p>
  
  <div class="content">
    <h3>Summary</h3>
    <p>{{content.summary}}</p>
    <p>{{content.details}}</p>
  </div>

  <div class="footer">
    Generated by MM Docs
  </div>
</body>
</html>
