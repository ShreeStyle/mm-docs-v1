<!DOCTYPE html>
<html>

<head>
    <style>
        {
                {
                    {
                    brandCSS
                }
            }
        }

        body {
            font-family: var(--font-primary), sans-serif;
            line-height: 1.6;
            padding: 50px;
            max-width: 800px;
            margin: 0 auto;
        }

        header {
            text-align: center;
            margin-bottom: 40px;
        }

        .logo {
            max-height: 100px;
        }

        h1 {
            color: var(--primary-color);
            font-size: 2.5em;
            margin-bottom: 10px;
        }

        h2 {
            color: var(--primary-color);
            margin-top: 30px;
        }

        .section {
            margin-bottom: 20px;
            padding: 20px;
            background: #fff;
            border-left: 5px solid var(--accent-color);
        }

        .pricing-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        .pricing-table th,
        .pricing-table td {
            padding: 12px;
            border: 1px solid #ddd;
        }

        .pricing-table th {
            background: var(--secondary-color);
            color: #fff;
        }
    </style>
</head>

<body>

    <header>
        {{#if brandKit.logo}}
        <img src="{{brandKit.logo}}" class="logo" />
        {{/if}}
        <h1>{{content.title}}</h1>
        <p>Prepared for: {{topic}}</p>
    </header>

    <section class="section">
        <h2>Executive Summary</h2>
        <p>{{content.executiveSummary}}</p>
    </section>

    <section>
        <h2>Methodology</h2>
        <ul>
            {{#each content.methodology}}
            <li>{{this}}</li>
            {{/each}}
        </ul>
    </section>

    <section>
        <h2>Investment</h2>
        <table class="pricing-table">
            <tr>
                <th>Item</th>
                <th>Price</th>
            </tr>
            {{#each content.pricing}}
            <tr>
                <td>{{this.item}}</td>
                <td>{{this.price}}</td>
            </tr>
            {{/each}}
        </table>
    </section>

    <section>
        <h2>Conclusion</h2>
        <p>{{content.conclusion}}</p>
    </section>

</body>

</html>