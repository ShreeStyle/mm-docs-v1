<!DOCTYPE html>
<html>

<head>
    <style>
        {
                {
                    {
                    brandCSS
                }
            }
        }

        body {
            font-family: var(--font-primary), sans-serif;
            padding: 40px;
            max-width: 800px;
            margin: 0 auto;
            line-height: 1.6;
        }

        .header {
            text-align: center;
            border-bottom: 3px solid var(--primary-color);
            padding-bottom: 20px;
            margin-bottom: 30px;
        }

        .name {
            font-size: 2.5em;
            color: var(--primary-color);
            margin: 0;
        }

        .contact {
            color: #666;
            margin-top: 10px;
        }

        .section {
            margin-bottom: 30px;
        }

        .section-title {
            color: var(--primary-color);
            font-size: 1.5em;
            border-bottom: 2px solid var(--accent-color);
            padding-bottom: 5px;
            margin-bottom: 15px;
        }

        .experience-item,
        .education-item {
            margin-bottom: 20px;
        }

        .job-title {
            font-weight: bold;
            color: var(--primary-color);
        }

        .company {
            font-style: italic;
            color: #555;
        }

        .date {
            color: #888;
            font-size: 0.9em;
        }

        .skills {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        .skill {
            background: var(--secondary-color);
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.9em;
        }
    </style>
</head>

<body>

    <div class="header">
        <h1 class="name">{{content.name}}</h1>
        <div class="contact">
            {{content.email}} | {{content.phone}} | {{content.location}}
        </div>
    </div>

    <div class="section">
        <h2 class="section-title">Professional Summary</h2>
        <p>{{content.summary}}</p>
    </div>

    <div class="section">
        <h2 class="section-title">Experience</h2>
        {{#each content.experience}}
        <div class="experience-item">
            <div class="job-title">{{this.title}}</div>
            <div class="company">{{this.company}} | <span class="date">{{this.period}}</span></div>
            <ul>
                {{#each this.responsibilities}}
                <li>{{this}}</li>
                {{/each}}
            </ul>
        </div>
        {{/each}}
    </div>

    <div class="section">
        <h2 class="section-title">Education</h2>
        {{#each content.education}}
        <div class="education-item">
            <strong>{{this.degree}}</strong> - {{this.institution}}<br>
            <span class="date">{{this.year}}</span>
        </div>
        {{/each}}
    </div>

    <div class="section">
        <h2 class="section-title">Skills</h2>
        <div class="skills">
            {{#each content.skills}}
            <span class="skill">{{this}}</span>
            {{/each}}
        </div>
    </div>

</body>

</html>